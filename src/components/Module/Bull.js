import styled from 'styled-components';
import {Colors}  from '../../components/System';

const Bull = styled.span`
  color: ${Colors.red};
  padding-right: 3px;
  font-weight: 400;
`;

export default Bull;
